"""
    Benchmark tests
"""

from pyssa.simulation import Simulation


# def test_benchmark(benchmark, setup_long):
#     V_r, V_p, X0, k = setup_long
#     sim = Simulation(V_r, V_p, X0, k, chem_flag=True)
#     benchmark(sim.simulate, max_t=1e5, max_iter=int(1e8))
